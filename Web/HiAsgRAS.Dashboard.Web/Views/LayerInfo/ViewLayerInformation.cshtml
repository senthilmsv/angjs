@model HiAsgRAS.ViewModel.LayerInfoModel

<input type="hidden" id="hdnSaveLayerUrl" value="@Url.Action("UpdateLayerList", "LayerInfo")" />
<input type="hidden" id="hdnLayerInfoAction" value="@Model.ActionMode" />
<input type="hidden" id="hdnSelectedRowId" value="@Model.Id" />
<input type="hidden" id="txtNewLayerId" value="@Model.NewLayerId" />
<input type="hidden" id="hdnCreatedBy" value="@Model.CreatedBy" />
<input type="hidden" id="hdnCreatedDate" value="@Model.CreatedDate" />


<div class="card">
    <div class="card-header" data-background-color="purple">
        <button type="button" class="close pull-right" data-dismiss="modal">&times;</button>
        <i class="fa fa-adjust" aria-hidden="true"></i> <h4 class="title" style="display: inline;">Layer Information</h4>

    </div>
    <div class="card-content">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div>


                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group label-floating">
                            <label class="control-label">Layer Name</label>
                            <input type="text" class="form-control" id="txtLayerName" maxlength="100" value="@Model.AppLayerName">

                        </div>
                    </div>
                  
                    <div class="col-md-6">
                        <div class="form-group label-floating">
                            <label class="control-label">Layer Location</label>
                            <input type="text" class="form-control" id="txtLayerLoc" maxlength="200"
                                    value="@Model.LayerLocation">
                        </div>
                    </div>
                   
                </div>

             
            @*    <div class="row">               
                   
 <div class="col-md-6">
                        <div class="form-group">
                            @{
            string yesChecked = "", noChecked = "";
            if (!string.IsNullOrEmpty(Model.IsActive) &&
                Model.IsActive.ToLower().Trim().Equals("no"))
            {
                 noChecked = "checked='checked'";
            }
            else
            {
                yesChecked = "checked='checked'";               
            }

            <div class="form-group label-floating">
                <label class="control-label">Is Active</label>
                <input type="radio" id="rdYes" name="rdActive" value="yes" style="width: 20px" @yesChecked />Yes &nbsp;
                <input type="radio" id="rdNo" name="rdActive" value="no" style="width: 20px" @noChecked>No
            </div>
                            }
                        </div>
                    </div>
                </div>*@
             

                <div class="row">
                    <div class="col-md-12">
                        <button type="button" class="btn btn-default pull-right" data-dismiss="modal" id="btnCancel ">Cancel</button>
                        <button type="button" class="btn btn-primary pull-right" id="btnLayerInfoUpdate">Update</button>
                    </div>
                </div>


                <div class="clearfix"></div>

                <!--Close div-->
            </div>
        }
    </div>
</div>

